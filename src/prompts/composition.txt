下面是一张或多张论文页面截图。请只提取其中的「化学成分表」（例如 Table … Chemical composition … wt.% 这类表格），忽略大段正文与其它图。

要求：
1. 用 Markdown 管道表格还原：表题可译成中文并保留英文原标题（表题中的 wt.%、mass% 等单位须一并照抄，勿省略）；列名译成中文（括号内可保留元素符号或英文缩写）；数值按图中原文抄写（含 “Bal.”“—”“–” 等）。**禁止** LaTeX（不要用 $、\pm 等）。**禁止 HTML 标签**（如 <sub>、<sup> 等），一律普通文本。
2. **列与格必须对齐**：表头各列顺序须与图中从左到右完全一致，再按行抄写；**禁止**把某一列的数字填到相邻列（例如不得把 Fe 列的数值抄到 Mn 列），**禁止**用正文、其它表格或示意图中的数字补全本表。
3. **空位与未添加元素**：图中为横线、空白或 “–”“—” 等表示「未添加 / 不适用」的，格内必须原样抄写为与图中一致的符号或横线；**禁止**用 0、极小数值或相邻格数字代替横线。
4. **易混数字**：仔细区分字形相近的数码与小数点（如 0.5 与 0.2、5 与 2）；以图中字形为准逐字抄写，**禁止**凭成分规律或常识改数。
5. 若有多张成分表，按表题分别各输出一张 Markdown 表。
6. 若没有任何化学成分表，只回复一行：未识别到化学成分表。

不要写与表格无关的说明。
