下面是一张或多张论文页面截图。请只提取其中的「力学性能数据表」（例如含 YS、UTS、屈服、抗拉、延伸率、El、E% 等列的 Table），不要描述应力应变曲线、硬度曲线等图，除非图中把数据明确做成表的一部分。

要求：
1. 用 Markdown 管道表格（| … |）还原：列名译成中文（括号内保留英文缩写如 YS、UTS）。
2. 单元格里的数字一律用**普通文本**：不确定度必须写成「346.5 ± 10.1」这种形式（数字、空格、Unicode 的 ±、数字）。**禁止**整段形如「美元符号 + 数字 + 反斜杠 pm + 数字 + 美元符号」的 LaTeX 写法。
3. **错误与正确对照（必须遵守）：**
   - 禁止：用「美元符号」包裹数字、中间用「反斜杠 + pm」写误差（与论文公式排版相同的那种写法，一律不要出现在表格单元格里）。
   - 必须：直接写「346.5 ± 10.1」「128 ± 20」——只用阿拉伯数字、空格和 Unicode 符号 ±。
4. **禁止任何 HTML 标签**（如 <sub>、<sup>、<table>、<tr>、<td> 等）。表头里的希腊字母或下标请用 Unicode（如 σ）或直接写成中文「抗拉强度/MPa」「延伸率/%」，不要用 HTML 做下标。
5. 若一页有多张力学表，按表题分别各输出一张 Markdown 表。
6. 若没有可识别的力学性能表，只回复一行：未识别到力学性能表。

不要写与表格无关的说明。
